package com.aviator.ketty.test;import com.googlecode.aviator.AviatorEvaluator;import java.util.HashMap;import java.util.Map;/** * @Author ketty bluce * @Create 2023/6/1 * @Version 1.0 */public class TestAviator1 {    public static void main(String[] args) {        Map<String, Object> env = new HashMap<>();        env.put("a", "a");        env.put("b", "b");        env.put("c", "c");        String script = "a!=null && b!=null && c!=null && !(a.isEmpty() && b.isEmpty() && c.isEmpty())";        Boolean result = (Boolean) AviatorEvaluator.execute(script, env);    }}