##脚本：TB≤L-LA-T-3
let errmsg = nil;
if(TB!= nil && L!= nil && LA!= nil && T!= nil){
   TB=decimal(TB);
   L=decimal(L);
   LA=decimal(LA);
   T=decimal(T);
   let L=L-LA-T-3;
   if(TB<=L){
       return true;
   }else{
       errmsg = "需满足公式 TB≤L-LA-T-3";
       return errmsg;
   }
}
return true;

